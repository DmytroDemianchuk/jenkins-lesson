agent {
        label 'main'
}
        stages {
         stage("create docker images") {
            steps {
                    echo " ======= start build images ======="
                    dir ('docker/toolbox') {
                            sh 'docker build. '
                    }
            }
        }
   }
